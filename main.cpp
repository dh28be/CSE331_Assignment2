#include <bits/stdc++.h>
#define N 200005
#define x first
#define y second

using namespace std;

typedef long long ll;
typedef pair<ll, ll> pll;

int main() {
    ios_base :: sync_with_stdio(false); 
    cin.tie(NULL);
    ll t, n, ans;
    cin >> t;
    while(t--) {
        cin >> n;
        cout << ans << '\n';
    }
    return 0;
}